kong:
  image: kong
  ports:
    - 8000:8000
    - 8443:8443
    - 8001:8001
    - 7946:7946
    - 7946:7946/udp
  environment:
    - KONG_DATABASE=postgres
    - KONG_PG_HOST=${kong_pg_host}
  logging:
    driver: awslogs
    options:
      awslogs-group: ${cloudwatch_log_group}
  stdin_open: true